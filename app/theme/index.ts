import { Icons } from '../assets';
import { styles } from './ApplicationStyles';
import { Color } from './Color';
import {
  horizontalScale,
  moderateScale,
  verticalScale,
  Metrics,
} from './Metrics';

export {
  Color,
  Icons,
  horizontalScale,
  verticalScale,
  moderateScale,
  styles,
  Metrics,
};
